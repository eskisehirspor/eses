# Minimal local config. Link a hosted project with `supabase link` before pushing migrations.

project_id = "eskisehirspor-app"

[api]
enabled = true

[db]
major_version = 15

[auth]
enabled = true
site_url = "http://localhost:3000"

[auth.email]
enable_signup = true
enable_confirmations = false
